---
description: 未定先问；细节 / idea / 技术栈写入对应文档
alwaysApply: true
---

# 项目约定

## 未定先问

除 `docs/details.md`、`docs/plan.md` 已写明的内容，以及用户明确说可以定的方案外：拿不定或有多方案利弊时必须先问。未让自由发挥时，不可想当然（例如擅自起名、选库、定交互却不说明这里自己做了主）。

## 文档同步

有新的已定细节、用户主动要求记录的 idea、或技术栈变动时，立刻写入对应文档。

- `docs/details.md`：已拍板的技术 / 产品细节（实现后也把实际约定补进去）
- `docs/idea.md`：只记用户明确要求留下的构想
- `docs/plan.md`：技术方案与技术栈矩阵

## 代码注释

- 项目源码（`src/`、`src-tauri/src/`）中的注释使用**简体中文**
- 只解释非显而易见的业务或技术细节；能自解释的代码不必加注释
- 框架 / 工具生成的样板（如 `vite-env.d.ts` 的 reference、lock 文件）不在此列
